# Адаптер Pixi.js-Skia
## Тестовое задание

```bash
# Установка зависимостей
npm i --prefix ./frontend
npm i --prefix ./backend

# Запуск фронтенда и бекенда
npm run dev --prefix ./frontend
npm run dev --prefix ./backend
```
